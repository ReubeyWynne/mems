# .NET build artifacts
bin/
obj/
.vs/
.vscode/
*.db
*.db-shm
*.db-wal

# Local cookies
cookies.txt
cookies2.txt

# Jekyll build output and cache
_site/
.jekyll-cache/

# AI tooling artifacts (untracked working files; never commit these)
.dsh/
.impeccable/
PRODUCT.md
.claude/
.cursor/
.ai/
.codex/
# Impeccable design skill (tracked) — but not its installed parser deps
.agents/skills/impeccable/node_modules/
