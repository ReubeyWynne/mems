@{
    var cls = Type == "error" ? "feedback-error" : "feedback-success";
}

<div id="feedback" class="@cls">@E(Message)</div>

@code {
    [Parameter] public string Type { get; set; } = "";
    [Parameter] public string Message { get; set; } = "";
}
